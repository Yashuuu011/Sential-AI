[phases.setup]
nixPkgs = ["python311Full", "gcc"]

[phases.install]
cmds = ["cd MalwareDetect && python -m pip install -r requirements.txt"]

[start]
cmd = "cd MalwareDetect && python -m uvicorn backend.main:app --host 0.0.0.0 --port ${PORT:-8000}"
